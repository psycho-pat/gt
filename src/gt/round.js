export class Round {
  constructor(name, idx, typ, typ2, max, points) {
    this.name = name;
    this.idx = idx;
    this.typ = typ;
	this.typ2 = typ2;
    this.max = max;
    this.points = points;
  }
}
